# ssh-tmux

Canonical SSH MCP backend for ContextForge.

This instance uses `mcp-ssh-tmux`, a stdio MCP server that manages persistent SSH sessions through local `tmux`. The duplicated `ssh-mcp` entries discovered in individual assistant configs are discovery evidence only and are superseded by this canonical service.


# Copy to contextforge.env for local client-harness tests only.
# Do not place ContextForge admin email/password or gateway bootstrap secrets here.
#
# This file is mounted into target-client containers. Values must be scoped to
# the exact virtual server under test and safe to revoke after the run.

CONTEXTFORGE_BEARER_TOKEN=
CONTEXTFORGE_SERVER_ID=
